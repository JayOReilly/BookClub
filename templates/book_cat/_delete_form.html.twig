<form method="post" action="{{ path('book_cat_delete', {'id': book_cat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book_cat.id) }}">
    <button class="btn">Delete</button>
</form>
